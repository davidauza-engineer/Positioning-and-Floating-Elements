linters:
    csslint:
    stylelint:
        fixer: true
        ignore: 'box-model'
    eslint:
      config: './eslint.config'
    phpcs:
      extensions: 'php,css'

files:
  ignore:
    - 'normalize.css'

fixers:
    enable: true
